<html>
    {{>head}}
    <body>
        {{>navbar}}
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    {{{body}}}
                </div>
            </div>
        </div>
        {{>body_javascript_includes}}
        {{#ifEq title "chat"}}
            {{>chatio}}
        {{/ifEq}}
    </body>
</html>
